/***************************************************************************
 # ReSTIRDIReferencePass/ColorType.slang
 # Thin stub: re-exports `ColorType` struct from the shared
 # `RenderPasses.PathTraceCommon.ColorType` module. The consumer plugin's
 # C++ still injects `COLOR_FORMAT` via `defines.add()`.
 *************************************************************************/
__exported import RenderPasses.PathTraceCommon.ColorType;
